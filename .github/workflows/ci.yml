name: Continuous Integration

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  static-analysis:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"
          cache: "pip"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          # Forced Exception: Install pytest 9.0.3 first
          python -m pip install pytest==9.0.3
          # Install the testing plugin WITHOUT dependencies to bypass the 9.0.0 pin
          python -m pip install --no-deps pytest-homeassistant-custom-component
          # Install remaining requirements, ignoring the pytest conflict
          python -m pip install -r requirements-dev.txt --ignore-installed pytest
          # Verify correct version is active
          pytest --version | grep "9.0.3"
      - name: Run Ruff lint
        run: python -m ruff check .
      - name: Run Ruff format check
        run: python -m ruff format --check .
      - name: Run mypy
        run: python -m mypy .
      - name: Check translation consistency
        run: python tools/check_translations.py

  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"
          cache: "pip"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          # Forced Exception: Install pytest 9.0.3 first
          python -m pip install pytest==9.0.3
          # Install the testing plugin WITHOUT dependencies to bypass the 9.0.0 pin
          python -m pip install --no-deps pytest-homeassistant-custom-component
          # Install remaining requirements, ignoring the pytest conflict
          python -m pip install -r requirements-dev.txt --ignore-installed pytest
          # Verify correct version is active
          pytest --version | grep "9.0.3"
      - name: Run tests with coverage
        run: |
          python -m pytest \
            tests/unit \
            tests/ha \
            --cov=custom_components/zurichsee_ha \
            --cov-report=xml:coverage.xml \
            --cov-report=term-missing
      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.xml
