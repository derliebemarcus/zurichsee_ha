name: Continuous Integration

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  static-analysis:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"
          cache: "pip"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          # Install dev requirements. We use --ignore-installed for pytest 
          # to resolve the conflict with pytest-homeassistant-custom-component (pins 9.0.0)
          # while satisfying security scanners that check requirements-dev.txt
          python -m pip install -r requirements-dev.txt --ignore-installed pytest==9.0.3 || true
          # Verify the installation
          pytest --version | grep "9.0.3"
      - name: Run Ruff lint
        run: python -m ruff check .
      - name: Run Ruff format check
        run: python -m ruff format --check .
      - name: Run mypy
        run: python -m mypy .
      - name: Check translation consistency
        run: python tools/check_translations.py

  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"
          cache: "pip"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install -r requirements-dev.txt
      - name: Run tests with coverage
        run: |
          python -m pytest \
            tests/unit \
            tests/ha \
            --cov=custom_components/zurichsee_ha \
            --cov-report=xml:coverage.xml \
            --cov-report=term-missing
      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.xml
